_# setup

1. Navigate to:

2. Rename the sample file:
- application.properties.sample → application.properties

3. Open `application.properties` and update:
- Database credentials
- sms credentials 
- Email credentials
- JWT secret
- otp.sms.enabled change to **true** whne sms provider availble_



